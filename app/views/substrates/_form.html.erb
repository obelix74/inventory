<%= form_with(model: substrate) do |form| %>
  <% if substrate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(substrate.errors.count, "error") %> prohibited this substrate from being saved:</h2>

      <ul>
        <% substrate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :subtype, style: "display: block" %>
    <%= form.text_field :subtype %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
