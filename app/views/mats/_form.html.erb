<%= form_with(model: mat) do |form| %>
  <% if mat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mat.errors.count, "error") %> prohibited this mat from being saved:</h2>

      <ul>
        <% mat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :outer, style: "display: block" %>
    <%= form.text_field :outer %>
  </div>

  <div>
    <%= form.label :inner, style: "display: block" %>
    <%= form.text_field :inner %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
