<%= form_with(model: print) do |form| %>
  <% if print.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(print.errors.count, "error") %> prohibited this print from being saved:</h2>

      <ul>
        <% print.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
